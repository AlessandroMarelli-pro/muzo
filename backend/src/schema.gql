# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

input AddTrackToPlaylistInput {
  position: Int
  trackId: Base64ID!
}

"""ID encoded as base64 for transport"""
scalar Base64ID

input CreateLibraryInput {
  autoScan: Boolean
  includeSubdirectories: Boolean
  maxFileSize: Int
  name: String!
  rootPath: String!
  scanInterval: Int
  supportedFormats: [String!]
}

input CreatePlaylistInput {
  description: String
  filters: PlaylistFilterInput
  isPublic: Boolean
  maxTracks: Int
  name: String!
  subgenreSelectionMode: String
}

type CursorPaginatedTracks {
  hasMore: Boolean!
  items: [Track!]
  nextCursor: Base64ID
}

input CursorPaginationArgs {
  cursor: Base64ID
  direction: String
  size: Int
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

input FilterCriteriaInput {
  acousticness: RangeInput
  arousalMood: [String!]
  artist: String
  atmosphereIds: [String!]
  danceabilityFeeling: [String!]
  genreIds: [Base64ID!]
  instrumentalness: RangeInput
  keyIds: [String!]
  libraryIds: [Base64ID!]
  liveness: RangeInput
  speechiness: RangeInput
  subgenreIds: [Base64ID!]
  tempo: RangeInput
  title: String
  valenceMood: [String!]
}

type FilterCriteriaResult {
  criteria: FilterCriteriaType!
  id: Base64ID!
  name: String!
}

type FilterCriteriaType {
  acousticness: Range
  arousalMood: [String!]
  artist: String
  atmosphereIds: [String!]
  danceabilityFeeling: [String!]
  genreIds: [Base64ID!]
  instrumentalness: Range
  keyIds: [String!]
  libraryIds: [Base64ID!]
  liveness: Range
  speechiness: Range
  subgenreIds: [Base64ID!]
  tempo: Range
  title: String
  valenceMood: [String!]
}

type FilterWithID {
  id: Base64ID
  name: String
}

type HomeMetrics {
  artistCount: Int!
  listeningStats: ListeningStats!
  recentActivity: [RecentActivity!]!
  topArtists: [TopArtist!]!
  topGenres: [TopGenre!]!
  totalListeningTime: Float!
  totalTracks: Int!
}

type Library implements Node {
  analyzedTracks: Float!
  createdAt: DateTime!
  failedTracks: Float!
  id: Base64ID!
  lastIncrementalScanAt: DateTime
  lastScanAt: DateTime
  name: String!
  pendingTracks: Float!
  rootPath: String!
  scanStatus: String!
  settings: LibrarySettings!
  totalTracks: Float!
  tracks(pagination: CursorPaginationArgs): CursorPaginatedTracks!
  updatedAt: DateTime
}

type LibrarySettings {
  autoScan: Boolean!
  includeSubdirectories: Boolean!
  maxFileSize: Float
  scanInterval: Float
  supportedFormats: String!
}

type ListeningStats {
  favoriteCount: Int!
  totalPlayTime: Float!
  totalPlays: Int!
}

type MusicPlayer {
  currentWaveformData(trackId: Base64ID!): [Float!]
}

type Mutation {
  addTrackToPlaylist(input: AddTrackToPlaylistInput!, playlistId: Base64ID!): PlaylistTrack!
  addTrackToQueue(trackId: Base64ID!): QueueItem!
  addTracksToQueue(trackIds: [Base64ID!]!): [QueueItem!]!
  authenticateSpotify(code: String!, codeVerifier: String!, userId: String!): SpotifyAuthResult!
  authenticateTidal(code: String!, codeVerifier: String!, userId: String!): TidalAuthResult!
  authenticateYouTube(code: String!, userId: String!): YouTubeAuthResult!
  caUpdatePlaylist(id: Base64ID!, input: UpdatePlaylistInput!): Playlist!
  createLibrary(input: CreateLibraryInput!): Library!
  createPlaylist(input: CreatePlaylistInput!): Playlist!
  createSavedFilter(input: SavedFilterInput!): FilterCriteriaResult!
  deleteLibrary(id: Base64ID!): Boolean!
  deletePlaylist(id: Base64ID!): Boolean!
  deleteSavedFilter(id: Base64ID!): Boolean!
  exportPlaylistToM3U(playlistId: Base64ID!): String!
  registerPlayedTrack(trackId: Base64ID!): Boolean!
  removeTrackFromPlaylist(playlistId: Base64ID!, trackId: Base64ID!): Boolean!
  removeTrackFromQueue(trackId: Base64ID!): RemoveTrackFromQueueResponse!
  resetQueue: Boolean!
  startLibraryScan(incremental: Boolean, libraryId: Base64ID!): Base64ID!
  stopLibraryScan(libraryId: Base64ID!, sessionId: Base64ID!): Boolean!
  syncPlaylistToSpotify(playlistId: ID!, userId: String!): ThirdPartySyncResult!
  syncPlaylistToTidal(playlistId: ID!, userId: String!): ThirdPartySyncResult!
  syncPlaylistToYouTube(playlistId: ID!, userId: String!): ThirdPartySyncResult!
  toggleBanger(trackId: Base64ID!): Track!
  toggleDislike(trackId: Base64ID!): Boolean!
  toggleFavorite(trackId: Base64ID!): Track!
  toggleLike(trackId: Base64ID!): Track!
  updatePlaylistSorting(input: UpdatePlaylistSortingInput!, playlistId: Base64ID!): PlaylistSorting!
  updatePlaylistTracksPositions(input: UpdatePlaylistPositionsInput!, playlistId: Base64ID!): Boolean!
  updateQueuePositions(input: UpdateQueuePositionsInput!): [QueueItem!]!
  updateSavedFilter(id: Base64ID!, input: SavedFilterInput!): FilterCriteriaResult!
}

interface Node {
  id: Base64ID!
}

type PaginatedTracks {
  items: [Track!]
  limit: Int!
  page: Int!
  pages: Int!
  total: Int!
}

input PaginationArgs {
  limit: Int
  offset: Int
  orderBy: String
  orderDirection: String
}

type Playlist implements Node {
  """True if the given track is already in this playlist"""
  containsTrack(trackId: Base64ID): Boolean
  createdAt: DateTime!
  createdById: Base64ID!
  description: String
  id: Base64ID!
  isPublic: Boolean!
  name: String!
  recommendations(limit: Int): [TrackRecommendation!]
  sorting: PlaylistSorting
  stats: PlaylistStats
  tracks: [PlaylistTrack!]
  updatedAt: DateTime
}

input PlaylistFilterInput {
  atmospheres: [String!]
  genreIds: [Base64ID!]
  libraryIds: [Base64ID!]
  subgenreIds: [Base64ID!]
  tempo: RangeInput
}

type PlaylistSorting {
  createdAt: DateTime!
  id: Base64ID!
  playlistId: Base64ID!
  sortingDirection: String!
  sortingKey: String!
  updatedAt: DateTime
}

type PlaylistStats {
  bpmRange: Range!
  energyRange: Range!
  genresCount: Int!
  images: [String!]!
  numberOfTracks: Int!
  subgenresCount: Int!
  topGenres: [String!]!
  topSubgenres: [String!]!
  totalDuration: Float!
}

type PlaylistTrack {
  addedAt: DateTime!
  id: Base64ID!
  playlistId: Base64ID!
  position: Int!
  track: Track
  trackId: Base64ID!
}

type PlaylistsResult {
  items: [Playlist!]!
}

type Query {
  getSpotifyAuthUrl: SpotifyAuthUrl!
  getTidalAuthUrl: TidalAuthUrl!
  getYouTubeAuthUrl: YouTubeAuthUrl!
  me: User!

  """
  Fetch any node by global ID. Use inline fragments (... on Playlist { ... }) to request fields.
  """
  node(id: Base64ID!): Node
}

type QueueItem {
  createdAt: DateTime!
  id: Base64ID!
  position: Int!
  track: Track
  trackId: Base64ID!
  updatedAt: DateTime
}

type RandomTrackWithStats {
  bangerCount: Float!
  dislikedCount: Float!
  likedCount: Float!
  remainingCount: Float!
  track: Track
}

type Range {
  max: Float!
  min: Float!
}

input RangeInput {
  max: Float
  min: Float
}

type RecentActivity {
  date: String!
  tracksAdded: Int!
  tracksAnalyzed: Int!
}

type RemoveTrackFromQueueResponse {
  artist: String
  success: Boolean!
  title: String
  trackId: Base64ID!
}

input SavedFilterInput {
  criteria: FilterCriteriaInput!
  isCurrent: Boolean
  name: String!
}

type SpotifyAuthResult {
  message: String
  success: Boolean!
}

type SpotifyAuthUrl {
  authUrl: String!
  codeVerifier: String!
}

type StaticFilterOptions {
  atmospheres: [FilterWithID!]!
  genres: [FilterWithID!]!
  keys: [FilterWithID!]!
  libraries: [FilterWithID!]!
  subgenres: [FilterWithID!]!
}

type ThirdPartySyncResult {
  errors: [String!]!
  playlistId: String
  playlistUrl: String
  skippedCount: Float!
  success: Boolean!
  syncedCount: Float!
}

type TidalAuthResult {
  message: String
  success: Boolean!
}

type TidalAuthUrl {
  authUrl: String!
  codeVerifier: String!
}

type TopArtist {
  artist: String!
  totalDuration: Float!
  trackCount: Int!
}

type TopGenre {
  genre: String!
  trackCount: Int!
}

type Track implements Node {
  aiAtmosphereKeywords: [String!]
  aiContextBackgrounds: String
  aiContextImpacts: String
  aiDescription: String
  aiTags: [String!]
  aiVocalsDesc: String
  aiVocalsDescriptions: String
  analysisStatus: String
  artist: String
  createdAt: DateTime
  date: DateTime
  duration: Float!
  fileCreatedAt: DateTime!
  fileName: String!
  filePath: String!
  fileSize: Float!
  format: String
  genres: [String!]
  id: Base64ID!
  imagePath: String
  isBanger: Boolean!
  isFavorite: Boolean!
  isLiked: Boolean!
  lastPlayedAt: DateTime
  lastScannedAt: DateTime
  libraryId: Base64ID
  listeningCount: Float!
  mfArousalMood: String
  mfDanceabilityFeeling: String
  mfKey: String
  mfTempo: Float
  mfValenceMood: String
  recommendations(limit: Int): [TrackRecommendation!]!
  subgenres: [String!]
  title: String
  updatedAt: DateTime
}

type TrackRecommendation {
  reasons: [String!]!
  similarity: Float!
  track: Track!
}

input UpdatePlaylistInput {
  description: String
  isPublic: Boolean
  name: String
}

input UpdatePlaylistPositionInput {
  id: Base64ID!
  position: Int!
}

input UpdatePlaylistPositionsInput {
  positions: [UpdatePlaylistPositionInput!]!
}

input UpdatePlaylistSortingInput {
  sortingDirection: String!
  sortingKey: String!
}

input UpdateQueuePositionInput {
  position: Int!
  trackId: Base64ID!
}

input UpdateQueuePositionsInput {
  positions: [UpdateQueuePositionInput!]!
}

type User implements Node {
  activeFilters: [FilterCriteriaResult!]
  currentFilter: FilterCriteriaResult
  email: String
  favorites: Playlist!
  firstName: String
  homeMetrics: HomeMetrics!
  id: Base64ID!
  lastName: String
  libraries: [Library!]!
  musicPlayer: MusicPlayer!
  paginatedTracks(pagination: PaginationArgs): PaginatedTracks!
  playlists: PlaylistsResult!
  queue: [QueueItem!]!
  randomTrackId: Base64ID!
  randomTrackWithStats: RandomTrackWithStats!
  recentlyPlayed: [Track!]!
  staticFilterOptions: StaticFilterOptions!
  tracks(pagination: CursorPaginationArgs): CursorPaginatedTracks!
}

type YouTubeAuthResult {
  message: String
  success: Boolean!
}

type YouTubeAuthUrl {
  authUrl: String!
}